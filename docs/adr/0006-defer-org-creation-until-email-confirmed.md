# 0006. 組織作成をメール確認後まで遅らせる

**状態**: 採用

## 背景

メール確認（`enable_confirmations`）を有効にすると `signUp()` はセッションを
返さない。当初の実装は、その状態でも直後に組織・メンバーシップを作っていた。

これが原因でメール確認を有効化できなかった。有効にすると次の状態が生まれる。

- 確認されなかった登録のぶんだけ、**誰も入れない組織が DB に残り続ける**
- 招待経路では `accepted_at` だけが立ち、**確認しないまま招待が消費される**

## 決定

**作成内容を `user_metadata` に預け、確認後の `/auth/callback` で消化する。**
確認が無効な場合は従来どおりその場で作成する。どちらの設定でも動く。

## 理由

検討した代替案は2つ。

1. **`auth.users.email_confirmed_at` を見る DB トリガー** — DB にビジネス
   ロジックが漏れる。ADR 0002 の「Service Layer を迂回する経路を作らない」に反する
2. **孤児組織を定期的に掃除するジョブ** — 対症療法で、招待が消費される問題は解けない

`user_metadata` は**クライアントから書き換えられる**領域なので、消化側で
預かった値を信用しない設計にした。招待はトークンで DB から引き直し、
確認済みメールとの一致と未所属であることを検証する。

## 落とし穴

**確認リンクの戻り先を redirect 許可リストに載せる必要がある。** 許可リストに
無い `redirect_to` はエラーにならず `site_url` に差し替えられるため、
載せ忘れると `/auth/callback` を通らず保留分が消化されない。実際に一度踏んだ。

## 関連

- [認証・認可モデル](../architecture/auth-and-authorization.md)
- [デプロイ手順](../deployment.md)
