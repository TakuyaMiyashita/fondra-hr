# 画面一覧

各フェーズで画面を実装する際に、状態定義を追記する。

## 凡例

各画面で以下の状態を定義する：

- **通常**: データが存在する場合の表示
- **ローディング**: データ取得中の Skeleton 表示
- **空状態**: データが0件の場合（アイコン + 説明 + CTA）
- **エラー**: データ取得失敗時の表示

---

## 認証（Phase 2）

| 画面 | パス | 状態定義 |
|------|------|----------|
| ログイン | `/login` | 通常: メール/パスワード入力フォーム。登録完了メッセージ（`?registered=true`）。送信中: ボタン disabled + Loader。エラー: toast |
| サインアップ | `/signup` | 通常: 組織名/メール/パスワード入力フォーム。送信中: ボタン disabled + Loader。エラー: toast |
| パスワードリセット | `/reset-password` | 通常: メール入力フォーム。送信完了: 成功メッセージ表示。送信中: ボタン disabled + Loader。エラー: toast |
| 招待承認 | `/invite/[token]` | 通常: 組織名・ロール表示 + パスワード入力。無効な招待: エラーメッセージ表示。送信中: ボタン disabled + Loader |

## アプリシェル（Phase 2）

| コンポーネント | 状態定義 |
|---------------|----------|
| サイドナビ | 展開/折りたたみ（アイコンのみ）切替。モバイル: Sheet で展開。ロールに応じたナビ項目出し分け（admin 以上で「設定」表示） |
| ヘッダー | 固定 (sticky)。SidebarTrigger + 組織スイッチャー + ダークモード切替 + ユーザーメニュー |
| 組織スイッチャー | 所属組織のドロップダウン。組織名 + プランバッジ + 現在の組織にチェックマーク。切替中: ボタン disabled |
| ユーザーメニュー | アバター（イニシャル）クリックでドロップダウン。メールアドレス表示 + ログアウト |
| ダークモード切替 | ライト/ダーク/システムの3択ドロップダウン |

## ダッシュボード（Phase 3-7）

| 画面 | パス | 状態定義 |
|------|------|----------|
| ダッシュボード | `/` | TBD |

## 従業員管理（Phase 3-1）

| 画面 | パス | 状態定義 |
|------|------|----------|
| 従業員一覧 | `/employees` | TBD |
| 従業員詳細 | `/employees/[id]` | TBD |
| 従業員作成 | Sheet/Dialog | TBD |
| 従業員編集 | Sheet/Dialog | TBD |

## 組織図（Phase 3-2）

| 画面 | パス | 状態定義 |
|------|------|----------|
| 組織図 | `/departments` | TBD |

## スキルマトリクス（Phase 3-3）

| 画面 | パス | 状態定義 |
|------|------|----------|
| スキルマトリクス | `/skills` | TBD |

## 1on1記録（Phase 3-4）

| 画面 | パス | 状態定義 |
|------|------|----------|
| 1on1一覧 | `/one-on-ones` | TBD |
| 1on1作成・編集 | Sheet/Dialog | TBD |

## 評価（Phase 3-5）

| 画面 | パス | 状態定義 |
|------|------|----------|
| 評価サイクル一覧 | `/evaluations` | TBD |
| 評価入力 | `/evaluations/[cycleId]/[employeeId]` | TBD |

## 監査ログ（Phase 3-6）

| 画面 | パス | 状態定義 |
|------|------|----------|
| 監査ログ一覧 | `/audit-logs` | TBD |

## 設定（Phase 2+）

| 画面 | パス | 状態定義 |
|------|------|----------|
| 組織設定 | `/settings` | TBD |
| メンバー管理 | `/settings/members` | TBD |

## ランディングページ（Phase 5）

| 画面 | パス | 状態定義 |
|------|------|----------|
| LP | `/` (未認証時) | TBD |
